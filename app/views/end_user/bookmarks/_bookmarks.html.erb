<% unless postProduct.end_user_id == current_end_user.id %>
  <% if postProduct.bookmarked_by?(current_end_user) %>
    <%= link_to "B外す", post_product_bookmarks_path(postProduct), method: :delete, remote: true %>
  <% else %>
    <%= link_to "B", post_product_bookmarks_path(postProduct), method: :post, remote: true %>
  <% end %>
<% end %>